{
  "name": "graphile-worker-publish-lambda",
  "version": "0.1.1",
  "description": "Types for the graphile-worker-publish Lambda function",
  "main": "build",
  "scripts": {
    "prepare": "npm run build",
    "prebuild": "../../node_modules/.bin/del build",
    "build": "../../node_modules/.bin/tsc",
    "prepublishOnly": "cd .. && npm run check-package-version",
    "postpublish": "cd .. && npm run release"
  },
  "files": [
    "build"
  ],
  "repository": "curvewise/graphile-worker-publish-lambda",
  "author": "Metabolize LLC",
  "license": "Apache-2.0"
}
